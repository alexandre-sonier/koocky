@import "a.kh"
@import "b.kh"

int main()
{
  int a = 1;
  test tutu;
  tutu.M_4test_variable_signed_int_1a = 2;
  @!(void)[bobo f :(int)[bobo.a] :(int)a];
  printf("%d + %d = %d\n", @!(int)[bobo.a], a, a + [bobo.a]);
  if ([test.b] > a)
    while ([test.b] > a + 80)
      if ([test.b] < 100)
	[test.b] += 1;
  printf("%d\n", @!(int)[bobo a :(int)10]);
  return 0;
}
