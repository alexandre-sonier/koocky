@import "a.kh"
@import "b.kh"

void	bla(test *tutu)
{
  [tutu.a] = 3;
}

int main()
{
  int a = 1;
  test *tutu;
  @!(void)[bobo f :(int)[bobo.a] :(int)a];
  printf("%d + %d = %d\n", @!(int)[bobo.a], a, a + [bobo.a]);
  if ([test.b] > a)
    while ([test.b] > a + 80)
      if ([test.b] < 100)
	[test.b] += 1;
  [tutu.a] = 2;
  printf("%d\n", @!(int)[tutu.a]);
  return 0;
}
