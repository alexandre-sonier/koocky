@import "A.kh"
@import "/usr/include/stdio.h"
@import "/usr/include/stdlib.h"

int	main(void)
{
  printf("%d\n", @!(int)[A.a]);		// affiche 42
  printf("%c\n", @!(char)[A.a]);	// affiche 'a'

  A*	myClass;

  printf("%d\n", @!(int)[A f]);

  return (0);
}
